import styled from 'styled-components';

const GoBackControl = styled.i.attrs({
  className: "fas fa-arrow-left"
})`
  padding: 10px;
  color: white;
  text-shadow: 0 0 4px black;
`;

export default GoBackControl;
