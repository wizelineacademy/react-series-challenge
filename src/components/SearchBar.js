import React from 'react';

import '../styles/SearchBar.css';

const SearchBar = () => {
  return (
    <div className="SearchBar">
      <input type="text" />
    </div>
  )
}

export default SearchBar;
