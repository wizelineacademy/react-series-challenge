import React from 'react'
import {
  StyledInput
} from './SearchBar.style'

const SearchBar = () => {
  return(
    <StyledInput
      placeholder="Search all the GIFs"
    />
  )
}

export default SearchBar
