import React from 'react';
import GifCard from './GifCard';

const GifsList = () => {

    return (
      <div>
          <h5>Gifs List</h5>
          <GifCard />
      </div>
    );

};

export default GifsList;
