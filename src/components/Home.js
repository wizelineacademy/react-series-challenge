import React from 'react';
import SearchBar from './SearchBar';
import GifList from './GifList';

const Home = (props) => (
    <div>
        <SearchBar/>
        <GifList/>
    </div>
);

export default Home;