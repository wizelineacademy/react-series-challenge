import React from 'react';
import SearchBar from './HomeSearchBar';
import GifList from './GifList';

const Home = () => (
    <div>
        <SearchBar/>
        <GifList/>
    </div>
);

export default Home;