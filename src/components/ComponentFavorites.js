import React, { Component, } from 'react';

export default class ComponentFavorites extends Component {
    render() {
        return (
            <div>
                Component Favorites
            </div>
        )
    }
}